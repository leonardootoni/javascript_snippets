const add = function(a, b) {
  return arguments;
};

console.log(add(2, 3));

const team = ["Tyler", "Porter"];
//print team size
const teamSize = team.length <= 4 ? "Team size: " + team.length : "Too many people on your team";
console.log(teamSize);
console.log("Oi Miriam!!!!");
